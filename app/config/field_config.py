# KVK is always formatted with 2 digits after the decimal point
KVK_DIGESTS_AFTER_DECIMAL = 2

# WGP is always formatted with 3 digits after the decimal point
WGP_DIGESTS_AFTER_DECIMAL = 3

# Article number is always 8 characters long
ARTICLE_NUMBER_LENGTH = 8